import { ICallback } from "./utils";
export default class App {
    private getContent(url);
    get(urls: string | Array<string>, callback: ICallback): void;
    download(urls: string | Array<string>, callback: ICallback): void;
}
